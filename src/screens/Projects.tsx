import { NativeBaseProvider, View, Text } from 'native-base';
import React from 'react';

const Projects = () => {
  return (
    <NativeBaseProvider>
      <View>
        <Text>Projects</Text>
      </View>
    </NativeBaseProvider>
  );
};

export default Projects;
